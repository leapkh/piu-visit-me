import 'package:flutter/material.dart';

class ProvincesScreen extends StatelessWidget {
  const ProvincesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Provinces Screen'),
    );
  }
}
